function remove_modal_window(element) {
    $('body').removeClass('esconder');
    $(element).remove();
};

// ================================= Atualiza o banco de dados conforme os dados da tela modal para uptade ============================== // 